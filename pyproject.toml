[project]
name = "Run scripts: Drivers and triggers of shut-in seismicity at the Coso geothermal field"
dependencies = [
    "openpyxl",
]

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
